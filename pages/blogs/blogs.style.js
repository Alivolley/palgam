import styled from '@emotion/styled';

const BlogsStyle = styled.div(() => ({
   '#headerGradient': {
      background: 'linear-gradient(180deg, #5922E9 0%, #DD43EF 100%)',
      opacity: '0.15',
      borderRadius: '1512px',
      filter: 'blur(200px)',
   },
}));

export default BlogsStyle;
